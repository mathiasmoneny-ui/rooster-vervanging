<!DOCTYPE html>
<html lang="nl">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Rooster Vervanging</title>
  <link href="https://fonts.googleapis.com/css2?family=DM+Sans:wght@300;400;500;600&family=DM+Mono:wght@400;500&display=swap" rel="stylesheet">
  <style>
    *, *::before, *::after { box-sizing: border-box; margin: 0; padding: 0; }

    :root {
      --bg: #F4F1EC;
      --surface: #FFFFFF;
      --surface2: #EDE9E2;
      --ink: #1A1814;
      --ink2: #6B6760;
      --ink3: #A8A49E;
      --accent: #2D5A3D;
      --accent2: #4A8C5C;
      --accent-light: #E8F0EB;
      --border: #D9D5CE;
      --red: #C0392B;
      --mono: 'DM Mono', monospace;
    }

    body {
      font-family: 'DM Sans', sans-serif;
      background: var(--bg);
      color: var(--ink);
      min-height: 100vh;
    }

    /* Header */
    header {
      background: var(--accent);
      padding: 28px 40px;
      display: flex;
      align-items: center;
      justify-content: space-between;
      position: sticky;
      top: 0;
      z-index: 100;
    }

    .logo {
      font-size: 18px;
      font-weight: 600;
      color: #fff;
      letter-spacing: -0.3px;
    }

    .logo span {
      font-weight: 300;
      opacity: 0.7;
    }

    .header-meta {
      font-family: var(--mono);
      font-size: 11px;
      color: rgba(255,255,255,0.5);
      letter-spacing: 0.05em;
    }

    /* Layout */
    main {
      max-width: 1200px;
      margin: 0 auto;
      padding: 48px 40px;
    }

    /* Tabs */
    .tabs {
      display: flex;
      gap: 4px;
      margin-bottom: 32px;
      background: var(--surface2);
      padding: 4px;
      border-radius: 10px;
      width: fit-content;
    }

    .tab {
      padding: 10px 24px;
      border: none;
      background: transparent;
      border-radius: 7px;
      font-family: 'DM Sans', sans-serif;
      font-size: 14px;
      font-weight: 500;
      color: var(--ink2);
      cursor: pointer;
      transition: all 0.2s;
    }

    .tab.active {
      background: var(--surface);
      color: var(--ink);
      box-shadow: 0 1px 4px rgba(0,0,0,0.08);
    }

    .tab:hover:not(.active) {
      color: var(--ink);
    }

    /* Section */
    .section { display: none; }
    .section.active { display: block; }

    .section-header {
      display: flex;
      align-items: baseline;
      justify-content: space-between;
      margin-bottom: 20px;
    }

    .section-title {
      font-size: 22px;
      font-weight: 600;
      letter-spacing: -0.5px;
      color: var(--ink);
    }

    .count-badge {
      font-family: var(--mono);
      font-size: 12px;
      color: var(--ink3);
      background: var(--surface2);
      padding: 3px 10px;
      border-radius: 20px;
    }

    /* Search */
    .search-bar {
      margin-bottom: 20px;
    }

    .search-bar input {
      width: 100%;
      max-width: 360px;
      padding: 10px 16px;
      border: 1px solid var(--border);
      border-radius: 8px;
      font-family: 'DM Sans', sans-serif;
      font-size: 14px;
      background: var(--surface);
      color: var(--ink);
      outline: none;
      transition: border-color 0.2s;
    }

    .search-bar input:focus {
      border-color: var(--accent2);
    }

    .search-bar input::placeholder {
      color: var(--ink3);
    }

    /* Table */
    .table-wrap {
      background: var(--surface);
      border-radius: 12px;
      border: 1px solid var(--border);
      overflow: hidden;
    }

    table {
      width: 100%;
      border-collapse: collapse;
    }

    thead {
      background: var(--surface2);
    }

    th {
      padding: 12px 20px;
      text-align: left;
      font-family: var(--mono);
      font-size: 11px;
      font-weight: 500;
      color: var(--ink3);
      letter-spacing: 0.06em;
      text-transform: uppercase;
      border-bottom: 1px solid var(--border);
    }

    td {
      padding: 14px 20px;
      font-size: 14px;
      color: var(--ink);
      border-bottom: 1px solid var(--border);
      vertical-align: middle;
    }

    tr:last-child td { border-bottom: none; }

    tr:hover td { background: #FAFAF8; }

    .cell-secondary {
      color: var(--ink2);
      font-size: 13px;
    }

    .cell-mono {
      font-family: var(--mono);
      font-size: 12px;
      color: var(--ink2);
    }

    /* Avatar */
    .avatar {
      width: 32px;
      height: 32px;
      border-radius: 50%;
      background: var(--accent-light);
      color: var(--accent);
      font-weight: 600;
      font-size: 13px;
      display: flex;
      align-items: center;
      justify-content: center;
      flex-shrink: 0;
    }

    .name-cell {
      display: flex;
      align-items: center;
      gap: 12px;
    }

    /* Hours badge */
    .hours-badge {
      display: inline-block;
      background: var(--accent-light);
      color: var(--accent);
      font-family: var(--mono);
      font-size: 11px;
      font-weight: 500;
      padding: 3px 8px;
      border-radius: 4px;
    }

    /* Status */
    .loading-state, .empty-state, .error-state {
      text-align: center;
      padding: 60px 20px;
      color: var(--ink3);
      font-size: 14px;
    }

    .error-state { color: var(--red); }

    .dot-loader {
      display: inline-flex;
      gap: 4px;
      margin-bottom: 12px;
    }

    .dot-loader span {
      width: 6px;
      height: 6px;
      border-radius: 50%;
      background: var(--ink3);
      animation: blink 1.2s ease-in-out infinite;
    }

    .dot-loader span:nth-child(2) { animation-delay: 0.2s; }
    .dot-loader span:nth-child(3) { animation-delay: 0.4s; }

    @keyframes blink {
      0%, 80%, 100% { opacity: 0.2; }
      40% { opacity: 1; }
    }

    /* Setup Banner */
    .setup-banner {
      background: #FFF8E1;
      border: 1px solid #FFD54F;
      border-radius: 10px;
      padding: 16px 20px;
      margin-bottom: 28px;
      font-size: 13px;
      color: #6D4C00;
      line-height: 1.6;
    }

    .setup-banner strong { font-weight: 600; }
    .setup-banner code {
      background: rgba(0,0,0,0.06);
      padding: 1px 5px;
      border-radius: 3px;
      font-family: var(--mono);
      font-size: 12px;
    }

    /* Responsive */
    @media (max-width: 700px) {
      main { padding: 24px 16px; }
      header { padding: 20px 16px; }
      th, td { padding: 10px 12px; }
      .hide-mobile { display: none; }
    }
  </style>
</head>
<body>

<header>
  <div class="logo">Rooster <span>Vervanging</span></div>
  <div class="header-meta" id="last-updated">—</div>
</header>

<main>

  <!-- Setup instructie (verwijder dit blok als alles werkt) -->
  <div class="setup-banner" id="setup-banner">
    <strong>⚙️ Eenmalige setup vereist:</strong> Vervang de twee Sheet-ID's hieronder in de code door jouw eigen Google Sheet ID's.
    Zie de instructies onderaan deze pagina.
  </div>

  <div class="tabs">
    <button class="tab active" onclick="switchTab('panden')">Panden</button>
    <button class="tab" onclick="switchTab('medewerkers')">Medewerkers</button>
  </div>

  <!-- Panden -->
  <div class="section active" id="tab-panden">
    <div class="section-header">
      <div class="section-title">Panden</div>
      <div class="count-badge" id="panden-count">—</div>
    </div>
    <div class="search-bar">
      <input type="text" id="search-panden" placeholder="Zoek op naam, adres of contactpersoon..." oninput="filterTable('panden')">
    </div>
    <div class="table-wrap">
      <div class="loading-state" id="panden-loading">
        <div class="dot-loader"><span></span><span></span><span></span></div>
        <div>Gegevens laden...</div>
      </div>
      <table id="panden-table" style="display:none">
        <thead>
          <tr>
            <th>Naam</th>
            <th>Adres</th>
            <th class="hide-mobile">Contactpersoon</th>
            <th class="hide-mobile">Telefoon</th>
            <th>Uren/week</th>
          </tr>
        </thead>
        <tbody id="panden-body"></tbody>
      </table>
      <div class="empty-state" id="panden-empty" style="display:none">Geen panden gevonden.</div>
      <div class="error-state" id="panden-error" style="display:none"></div>
    </div>
  </div>

  <!-- Medewerkers -->
  <div class="section" id="tab-medewerkers">
    <div class="section-header">
      <div class="section-title">Medewerkers</div>
      <div class="count-badge" id="medewerkers-count">—</div>
    </div>
    <div class="search-bar">
      <input type="text" id="search-medewerkers" placeholder="Zoek op naam of e-mailadres..." oninput="filterTable('medewerkers')">
    </div>
    <div class="table-wrap">
      <div class="loading-state" id="medewerkers-loading">
        <div class="dot-loader"><span></span><span></span><span></span></div>
        <div>Gegevens laden...</div>
      </div>
      <table id="medewerkers-table" style="display:none">
        <thead>
          <tr>
            <th>Naam</th>
            <th class="hide-mobile">Telefoon</th>
            <th>E-mailadres</th>
          </tr>
        </thead>
        <tbody id="medewerkers-body"></tbody>
      </table>
      <div class="empty-state" id="medewerkers-empty" style="display:none">Geen medewerkers gevonden.</div>
      <div class="error-state" id="medewerkers-error" style="display:none"></div>
    </div>
  </div>

</main>

<script>
  // ============================================================
  // STAP 1: Vervang deze ID's door jouw eigen Google Sheet ID's
  // ============================================================
  const SHEET_ID_PANDEN      = 'JOUW_SHEET_ID_PANDEN_HIER';
  const SHEET_ID_MEDEWERKERS = 'JOUW_SHEET_ID_MEDEWERKERS_HIER';
  // ============================================================

  const GID = '0'; // Eerste tabblad van de sheet

  let allPanden = [];
  let allMedewerkers = [];

  function sheetUrl(id) {
    return `https://docs.google.com/spreadsheets/d/${id}/gviz/tq?tqx=out:json&gid=${GID}`;
  }

  async function fetchSheet(id) {
    const res = await fetch(sheetUrl(id));
    const text = await res.text();
    const json = JSON.parse(text.substring(47, text.length - 2));
    const rows = json.table.rows;
    const cols = json.table.cols.map(c => c.label);
    return rows.map(row => {
      const obj = {};
      row.c.forEach((cell, i) => { obj[cols[i]] = cell ? (cell.v ?? cell.f ?? '') : ''; });
      return obj;
    });
  }

  function initials(name) {
    return String(name).split(' ').map(w => w[0]).join('').substring(0, 2).toUpperCase();
  }

  function show(id) { document.getElementById(id).style.display = ''; }
  function hide(id) { document.getElementById(id).style.display = 'none'; }

  async function loadPanden() {
    try {
      const data = await fetchSheet(SHEET_ID_PANDEN);
      allPanden = data;
      renderPanden(allPanden);
    } catch (e) {
      hide('panden-loading');
      show('panden-error');
      document.getElementById('panden-error').textContent =
        'Kon de pandenlijst niet laden. Controleer of het Sheet ID correct is en de sheet openbaar is.';
    }
  }

  async function loadMedewerkers() {
    try {
      const data = await fetchSheet(SHEET_ID_MEDEWERKERS);
      allMedewerkers = data;
      renderMedewerkers(allMedewerkers);
    } catch (e) {
      hide('medewerkers-loading');
      show('medewerkers-error');
      document.getElementById('medewerkers-error').textContent =
        'Kon de medewerkerslijst niet laden. Controleer of het Sheet ID correct is en de sheet openbaar is.';
    }
  }

  function renderPanden(data) {
    hide('panden-loading');
    const tbody = document.getElementById('panden-body');
    tbody.innerHTML = '';
    if (!data.length) { show('panden-empty'); return; }
    hide('panden-empty');
    show('panden-table');
    document.getElementById('panden-count').textContent = data.length + ' panden';
    data.forEach(row => {
      const keys = Object.keys(row);
      const naam = row[keys[0]] || '';
      const adres = row[keys[1]] || '';
      const contact = row[keys[2]] || '';
      const tel = row[keys[3]] || '';
      const uren = row[keys[4]] || '';
      const tr = document.createElement('tr');
      tr.innerHTML = `
        <td><div class="name-cell"><div class="avatar">${initials(naam)}</div><span>${naam}</span></div></td>
        <td class="cell-secondary">${adres}</td>
        <td class="cell-secondary hide-mobile">${contact}</td>
        <td class="cell-mono hide-mobile">${tel}</td>
        <td>${uren ? `<span class="hours-badge">${uren}u</span>` : '—'}</td>
      `;
      tbody.appendChild(tr);
    });
  }

  function renderMedewerkers(data) {
    hide('medewerkers-loading');
    const tbody = document.getElementById('medewerkers-body');
    tbody.innerHTML = '';
    if (!data.length) { show('medewerkers-empty'); return; }
    hide('medewerkers-empty');
    show('medewerkers-table');
    document.getElementById('medewerkers-count').textContent = data.length + ' medewerkers';
    data.forEach(row => {
      const keys = Object.keys(row);
      const naam = row[keys[0]] || '';
      const tel = row[keys[1]] || '';
      const email = row[keys[2]] || '';
      const tr = document.createElement('tr');
      tr.innerHTML = `
        <td><div class="name-cell"><div class="avatar">${initials(naam)}</div><span>${naam}</span></div></td>
        <td class="cell-mono hide-mobile">${tel}</td>
        <td class="cell-mono">${email ? `<a href="mailto:${email}" style="color:var(--accent2);text-decoration:none">${email}</a>` : '—'}</td>
      `;
      tbody.appendChild(tr);
    });
  }

  function filterTable(type) {
    const query = document.getElementById(`search-${type}`).value.toLowerCase();
    const data = type === 'panden' ? allPanden : allMedewerkers;
    const filtered = data.filter(row =>
      Object.values(row).some(v => String(v).toLowerCase().includes(query))
    );
    if (type === 'panden') renderPanden(filtered);
    else renderMedewerkers(filtered);
  }

  function switchTab(tab) {
    document.querySelectorAll('.tab').forEach(t => t.classList.remove('active'));
    document.querySelectorAll('.section').forEach(s => s.classList.remove('active'));
    event.target.classList.add('active');
    document.getElementById(`tab-${tab}`).classList.add('active');
  }

  // Tijdstip bijwerken
  document.getElementById('last-updated').textContent =
    'Bijgewerkt: ' + new Date().toLocaleTimeString('nl-NL', { hour: '2-digit', minute: '2-digit' });

  loadPanden();
  loadMedewerkers();
</script>

</body>
</html>
